---
title: Identifying Latent Concepts and Structures for Generalized Category Discovery
title_zh: 面向广义类别发现的潜在概念与结构识别
authors: "Boyang Dai, Chaoqi Chen, Yizhou Yu"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00620v1"
tags: ["query:cgcd"]
score: 10.0
evidence: 直接处理广义类别发现(GCD)，在开放世界中识别已知类并发现新类
tldr: 传统广义类别发现(GCD)方法只关注聚类目标，忽视了视觉基础模型产生的高秩、纠缠的标记表示，这不利于无监督地发现潜在概念和结构。本文提出组合基元场(CPF-GCD)框架，通过强制低秩组合组织来重塑特征空间，使潜在结构可识别，并假设所有类别均可表达为共享基元的组合。在多个基准上，CPF-GCD能同时有效识别已知类并发现新类，显著优于现有方法，为开放世界类别发现提供了新的表示学习视角。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00620v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 863, \"height\": 421, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00620v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1678, \"height\": 849, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00620v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 808, \"height\": 425, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00620v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 823, \"height\": 259, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00620v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 836, \"height\": 358, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00620v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 812, \"height\": 395, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00620v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 707, \"height\": 414, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00620v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1602, \"height\": 897, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00620v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1600, \"height\": 881, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00620v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 814, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00620v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 788, \"height\": 339, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00620v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 741, \"height\": 262, \"label\": \"Table\"}]"
motivation: 直接处理广义类别发现(GCD)，在开放世界中识别已知类并发现新类。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
广义类别发现（GCD）旨在开放世界环境中识别已知类别并自主发现新类别。然而，当前方法主要聚焦于设计聚类目标，往往忽视了一个关键瓶颈：标准视觉骨干网络产生高秩、纠缠的 token 表示，不适合无监督地发现潜在概念和结构。本文提出组合基元场（CPF-GCD），一种新颖的表示学习框架，通过强制低秩组合组织来重塑特征空间，使此类潜在结构可辨识。我们核心假设是，所有类别，无论已知还是新类，都可以表示为一组有限的可学习视觉基元的组合和空间排列，这些基元捕捉了可复用的概念。CPF 通过空间场机制实例化这种几何约束。它插入骨干网络和头部之间，通过低秩基元混合重写带噪声的 patch token，从而将图像有效分解为可复用的原子部分及其空间布局。通过显式建模基元的空间分布，CPF 使新类别能够自然地作为共享词汇表上的新激活模式涌现。这将表示的重点从单纯划分全局嵌入转移到构建结构化且可分离的基元场。大量实验表明，CPF 作为一个通用的即插即用模块，在不同 GCD 基线方法上均持续提升性能，验证了识别并利用低秩组合结构是开放世界识别的一个关键归纳偏置。

## Abstract
Generalized Category Discovery (GCD) aims to recognize known classes while autonomously discovering novel ones in open-world settings. However, current approaches primarily focus on designing clustering objectives, often overlooking a critical bottleneck: standard vision backbones yield high-rank, entangled token representations that are ill-suited for unsupervised discovery of latent concepts and structures. In this paper, we propose Compositional Primitive Fields (CPF-GCD), a novel representation learning framework that reshapes the feature space to make such latent structure identifiable by enforcing a low-rank compositional organization. Our core hypothesis is that all categories, whether known or novel, can be expressed as compositions and spatial arrangements of a finite set of learnable visual primitives that capture reusable concepts. CPF instantiates this geometric constraint via a spatial field mechanism. Inserted between the backbone and the head, it rewrites noisy patch tokens through low-rank primitive mixtures, effectively decomposing images into reusable atomic parts and their spatial layouts. By explicitly modeling the spatial distribution of primitives, CPF enables novel categories to emerge naturally as new activation patterns over a shared vocabulary. This shifts the focus of representation from merely partitioning global embeddings to constructing a structured and separable primitive field. Extensive experiments demonstrate that CPF serves as a generic, plug-and-play module that consistently boosts performance across diverse GCD baselines, validating that identifying and leveraging low-rank compositional structure is a crucial inductive bias for open-world recognition.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

-   **研究动机**：广义类别发现（GCD）要求模型在开放世界中同时识别已标注的已知类别和无监督地发现未标注的新类别。现有方法主要关注设计聚类目标和发现头，忽视了 **骨干网络输出表示的质量** 这一瓶颈：标准视觉 Transformer（如 DINO ViT）产生的 `[CLS]` token 与 patch tokens 呈高秩、纠缠状态，全局描述子丢弃了细粒度或新类所需的局部证据，而原始 patch tokens 又充满冗余噪声和虚假相关。
-   **核心思想**：论文认为**所有类别（已知与新类）均可表达为一组有限的可学习视觉基元（primitives）的组合与空间排列**。因此，不应仅在输出头做聚类，而应先重塑骨干与头之间的表示接口，使潜在结构可辨识。
-   **整体含义**：提出**组合基元场（Compositional Primitive Fields，CPF-GCD）**，作为一个通用、即插即用的模块，插入骨干网络与任何 GCD 头之间，通过低秩基元混合重写 patch tokens，构建一个结构化、紧凑的基元场。这样，新类别可以自然地作为共享基元词汇表上的新激活模式涌现，而不是需要全新的特征轴。

## 2. 方法论：核心思想与关键技术细节

-   **核心机制**：CPF 将骨干输出的 patch token 矩阵 `X ∈ ℝ^(N×D)` 近似为 `X ≈ A P`，其中 `P ∈ ℝ^(M×D)` 是一组可学习的基元（`M ≪ min(N,D)`），`A ∈ ℝ^(N×M)` 是 token-to-primitive 软分配矩阵。这个低秩瓶颈抑制了冗余变动，只保留反复出现的语义结构。
-   **整体流程**：CPF 由三阶段构成，置于骨干与 GCD 头之间，不修改下游头或损失函数。
    1.  **图像条件基元码本构建**（Image-Conditioned Primitive Codebook）：
        - 维护一个全局共享的基元基 `P_base`，并通过图像条件映射产生偏移 `ΔP`，得到当前图像的适用码本 `P = P_base + ΔP`。
        - 投影 tokens 并计算兼容性得分 `S`，通过按基元维度列归一化的 softmax 得到先验分配 `A_prior`（每列和为 1，促使基元竞争重要的 patch）。
    2.  **基元证据精炼**（Primitive Evidence Refinement）：
        - 用 `A_prior` 聚合 token 证据得到初始基元状态 `P_0 = A_prior^⊤ X`。
        - 执行 **token 条件更新**：以 `P_0` 为 query，原始 tokens 为 key/value，通过交叉注意力得到修正项，残差更新得到 `P_1`。
        - 执行 **基元状态整合**：在各基元状态 `P_1` 间使用自注意力进行交互，得到最终精炼的基元状态 `P_2`，消除冗余并提升一致性。
    3.  **分配融合与令牌重写**（Assignment Fusion and Token Rewriting）：
        - 从精炼阶段的交叉注意力权重中提取数据分配 `A_data`（平均多头权重并沿基元维 softmax）。
        - 在 log-potential 空间，用可学习标量权重 `α_prior` 和 `α_data` 融合先验分配与数据分配，得到最终分配矩阵 `A`。
        - **基元到令牌解码**：将精炼基元 `P_2` 经 MLP 映射为 `P_refined`，计算基元混合信号 `A·P_refined`，再经 MLP 并残差连接至原始 `X`，得到重构后的 token 矩阵 `X_refined`。
    - **与 GCD 头集成**：更新类 token `[CLS]* = [CLS] + Mean(X_refined)`，然后送入标准的 GCD 头（原型、聚类或对比学习），全程不改变下游模块。

## 3. 实验设计

-   **数据集与场景**：
    - **细粒度基准**：CUB-200 (鸟类)、Stanford Cars (汽车)、FGVC Aircraft (飞行器)。
    - **粗粒度基准**：CIFAR-10、CIFAR-100、ImageNet-100。
    - 严格遵循 Vaze et al. 的类别划分与评测协议。
-   **评价指标**：聚类准确率（ACC），分别报告已知/旧类（Old）、新类（New）与全部类别（All）的数值，利用匈牙利算法进行匹配。
-   **基线方法**：
    - **作为即插即用载体的主流范式**：SimGCD（原型学习）、LegoGCD（原型学习）、CMS（对比学习）、SelEx（对比学习）。将 CPF 注入这四种方法并对比原版。
    - **更广泛的比较对象**：包括 GCD、μGCD、PromptCAL、DCCL、InfoSieve、AMEND、PIM、ProtoGCD、APL、ConGCD 等，在表格中一并列出性能。

## 4. 资源与算力

-   **特征提取器**：使用在 ImageNet-1K 上预训练并冻结的 **DINO ViT-B/16**。
-   **计算开销对比**：论文在 Table 5 中给出了以 SimGCD 为宿主的参数与时间对比：CPF 增加约 **7.5M 参数**，训练时间增加约 **8%**，推理时间增加约 **6%**，表明模块轻量且高效。
-   **硬件环境**：**论文未明确说明**所用的 GPU 型号、数量或绝对训练时长，只提供了相对开销百分比。

## 5. 实验数量与充分性

-   **主要实验**：在 **6 个数据集**（3 细粒度 + 3 粗粒度）上，对 **4 种不同范式的基线方法** 进行即插即用测试，总计 **24 组** 主实验对比（每个数据集 × 每种方法），同时列出大量现有方法的性能作为参照。
-   **消融研究**：
    - **关键组件消融**：逐步移除数据分配（`A_data`）、先验分配（`A_prior`）、可学习融合门控、以及仅用原始 patch 特征，在三个细粒度数据集上验证各部分贡献。
    - **超参数敏感性**：在 CUB、Cars、Aircraft 上，对基元数量 `M` 与注意力头数 `H` 进行网格分析（8 ∼ 24），证明 `M ∈ [12,16]` 时性能稳定，鲁棒性好。
-   **深度分析实验**：
    - 几何分析（有效秩、冯·诺依曼熵）对比 CPF 与 SelEx 的特征分布。
    - 类别数估计误差对比（CIFAR-100、ImageNet-100、CUB、Cars）。
    - 注意力图与基元激活的可视化，展示语义可解释性。
-   **公平性与充分性**：实验严格遵循原有基线方法的超参与训练流程，不引入方法特定调参；横向对比涵盖多种 GCD 设计，消融与分析多角度佐证，实验设计**比较充分且客观公平**。

## 6. 主要结论与发现

1.  **即插即用的通用增益**：CPF 在不修改任何下游头或损失的情况下，能一致提升原型、聚类、对比等不同范式 GCD 方法的性能。在细粒度数据集上，新类平均准确率提升达 **4.05%（CUB）**、**1.85%（Aircraft）**、**6.00%（Stanford Cars）**；粗粒度数据集上同样稳健。
2.  **几何结构改善**：CPF 显著降低了特征空间的有效秩和冯·诺依曼熵，将高秩、纠缠的表示转化为更紧凑、结构化的基元场，滤除了冗余噪声。
3.  **语义可解释性**：可视化显示，基元捕捉到跨类别、可复用的部件（如头部、躯干、肢体），新类别表现为这些基元的新激活模式；注意力图更聚焦于前景目标。
4.  **类别数估计更准确**：CPF 辅助下的模型给出的新类数量估计误差远低于基线方法。
5.  **轻量高效**：仅增加约 7.5M 参数，时间开销增幅 ≤ 8%，几乎不影响部署效率。

## 7. 优点

-   **视角新颖**：首次明确指出 GCD 中**骨干-头界面表示的质量**是关键瓶颈，并将问题转化为令牌结构的重塑，而非单纯优化聚类目标。
-   **方法简洁优雅**：低秩基元场的设计巧妙，通过 `X ≈ A P` 一次性完成降噪、去纠缠和结构化，理论扎实且易于理解。
-   **通用性与非侵入性**：作为一个即插即用模块，可无缝接入任何现有 GCD 流水线，不改变原有架构与训练策略，极大降低了应用门槛。
-   **实验扎实全面**：多数据集、多基线、多范式交叉验证，消融与分析深入，从数值、几何、可视化多角度佐证结论，说服力强。
-   **可解释性强**：基元激活模式和注意力可视化直观展示了模型如何通过可复用部件组合来识别新旧类别，增强了方法的透明度。

## 8. 不足与局限

-   **粗粒度与已知类的微小波动**：在部分粗粒度数据集上，旧类的准确率出现轻微负增长（如 ImageNet-100 上 LegoGCD 旧类下降 1.4%），可能表示基元场在已知类上重新分配了部分表示容量。
-   **超参数仍然存在**：虽然基元数量 `M` 在一定范围内鲁棒，但仍需设定，且论文将其与注意力头数 `H` 耦合（`M = H`），实际应用前可能需要少量域适应性的选择。
-   **骨干网络的局限**：实验仅基于冻结的 ViT-B/16，未验证在其他视觉骨干（如更大/更小的 ViT、CNN 或多模态模型）上的通用性。
-   **场景覆盖**：数据集均为平衡的标准视觉分类，缺少对长尾分布、少样本或极度开放动态环境（如类别数未知且持续增长）的验证。
-   **算力透明度**：论文没有说明训练使用的具体 GPU 型号和绝对训练时间，仅给出了相对开销，硬件需求难以精确评估。

（完）
